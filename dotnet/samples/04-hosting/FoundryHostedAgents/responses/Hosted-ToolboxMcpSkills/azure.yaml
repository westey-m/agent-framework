# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: hosted-toolbox-mcp-skills
services:
    ai-project:
        host: azure.ai.project
    hosted-toolbox-mcp-skills:
        project: .
        host: azure.ai.agent
        language: csharp
        uses:
            - ai-project
        codeConfiguration:
            dependencyResolution: remote_build
            entryPoint: HostedToolboxMcpSkills.dll
            runtime: dotnet_10
        # ASPNETCORE_URLS pins the listen port. Source deploy runs this project as a plain ASP.NET
        # app, and the .NET base image defaults it to port 80, while Foundry probes port 8088 for
        # readiness, so without it every invoke fails with HTTP 424 session_not_ready. Recent
        # Microsoft.Agents.AI.Foundry.Hosting versions bind the port themselves and take precedence
        # over this value, so it only matters when the project is pinned to an older package.
        env:
            ASPNETCORE_URLS: http://+:8088
            AZURE_AI_MODEL_DEPLOYMENT_NAME: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
            TOOLBOX_NAME: ${TOOLBOX_NAME}
        container:
            resources:
                cpu: "0.5"
                memory: 1Gi
        description: |
            A hosted agent that discovers MCP-based skills attached to a Foundry Toolbox and serves them at runtime.
        kind: hosted
        metadata:
            tags:
                - AI Agent Hosting
                - Azure AI AgentServer
                - Agent Framework
        name: hosted-toolbox-mcp-skills
        protocols:
            - protocol: responses
              version: 2.0.0