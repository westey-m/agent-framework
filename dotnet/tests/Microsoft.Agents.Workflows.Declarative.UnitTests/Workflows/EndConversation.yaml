kind: Workflow
trigger:

  kind: OnConversationStart
  id: my_workflow
  actions:

    - kind: EndConversation
      id: end_all

    - kind: SendActivity
      id: send_activity_1
      activity: NEVER 1!
